#!/usr/bin/python3
if __name__ == "__main__":
    import hidden_4
    name = dir(hidden_4)
    for name in range(len(name)):
        if name[i][0] != '-' and name[i][1] != '-':
            print(name[i])
